import fs from 'fs'; const env = fs.readFileSync('.env', 'utf8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const keyMatch = env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/); const key = keyMatch ? keyMatch[1].trim() : env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1].trim(); fetch(url + '/rest/v1/projects?select=*&order=created_at.desc&limit=2', { headers: { apikey: key, Authorization: 'Bearer ' + key } }).then(r => r.json()).then(console.log);
